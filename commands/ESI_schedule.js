module.exports = {
    name: 'schedule',
    description: 'track ESI calendar match, send ping to all involved 1 hour before match time',
    execute(message, args) {
        /*
            args must contain: host, commentator, two runners, month, day, hour, minute (optional)
        */
    }
}